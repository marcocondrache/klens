[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
clap = { version = "4", features = ["derive"] }
klens = { path = "..", features = ["typescript"] }
ts-rs = "12.0.1"
xshell = "0.2"
