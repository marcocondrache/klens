# Schema: https://json.schemastore.org/cargo-mutants-config.json
test_tool = "nextest"

exclude_globs = [
    # Only compiled with the `ui` feature, which needs the built web bundle.
    "src/server/web.rs",
    "src/main.rs",
]
